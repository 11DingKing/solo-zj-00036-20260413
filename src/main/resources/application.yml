spring:
  profiles:
    active: @spring.profiles.active@

app:
  datasource:
    jdbc-url: ${DB_URL:jdbc:postgresql://localhost:5432/amigoscodedemo}
    username: ${DB_USERNAME:postgres}
    password: ${DB_PASSWORD:password}
    pool-size: 30